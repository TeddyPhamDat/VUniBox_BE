namespace VUniBox.Models.Enum
{
    public enum DocumentStatus
    {
        Saved = 1,      // Đã lưu vào folder theo type
        Trash = 2,      // Trong thùng rác
        Deleted = 3,    // Đã xóa vĩnh viễn
        Processing = 4  // Đang xử lý
    }
}

